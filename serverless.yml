service: synk

provider:
  name: google
  runtime: nodejs
  project: ${env:PROJECT}
  credentials: ${file(./home.js):theGoogleCloudCredentialFile}

plugins:
  - serverless-google-cloudfunctions

package:
  include:
    - first/**

functions:
  first:
    handler: http
    events:
      - http: path
